<?php
/**
 * @var \Phalcon\Mvc\View\Engine\Php $this
 */
?>

<?php use Phalcon\Tag; ?>

<div class="row">
    <nav>
        <ul class="pager">
            <li class="previous"><?php echo $this->tag->linkTo(array("backend/users/index", "Go Back")); ?></li>
            <li class="next"><?php echo $this->tag->linkTo(array("backend/users/new", "Create ")); ?></li>
        </ul>
    </nav>
</div>

<div class="page-header">
    <h1>Search result</h1>
</div>

<?php echo $this->getContent(); ?>

<div class="row">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
            <th>Username</th>
            <th>Password</th>
            <th>Datecreate</th>
            <th>Is Of Active</th>
            <th>Is Of Del</th>
            <th>Room</th>
            <th>Desc</th>

                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($page->items as $user): ?>
            <tr>
                <td><?php echo $user->id ?></td>
            <td><?php echo $user->username ?></td>
            <td><?php echo $user->password ?></td>
            <td><?php echo $user->datecreate ?></td>
            <td><?php echo $user->is_active ?></td>
            <td><?php echo $user->is_del ?></td>
            <td><?php echo $user->room ?></td>
            <td><?php echo $user->desc ?></td>

                <td><?php echo $this->tag->linkTo(array("backend/users/edit/" . $user->id, "Edit")); ?></td>
                <td><?php echo $this->tag->linkTo(array("backend/users/delete/" . $user->id, "Delete")); ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="row">
    <div class="col-sm-1">
        <p class="pagination" style="line-height: 1.42857;padding: 6px 12px;">
            <?php echo $page->current, "/", $page->total_pages ?>
        </p>
    </div>
    <div class="col-sm-11">
        <nav>
            <ul class="pagination">
                <li><?php echo $this->tag->linkTo("backend/users/search", "First") ?></li>
                <li><?php echo $this->tag->linkTo("backend/users/search?page=" . $page->before, "Previous") ?></li>
                <li><?php echo $this->tag->linkTo("backend/users/search?page=" . $page->next, "Next") ?></li>
                <li><?php echo $this->tag->linkTo("backend/users/search?page=" . $page->last, "Last") ?></li>
            </ul>
        </nav>
    </div>
</div>
